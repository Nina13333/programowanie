#include <iostream>
using namespace std;
int main(){
	int liczba;
	cout << "Podaj liczbe: ";
	cin >> liczba;
	// Operator AND (&&)
	if ((liczba >=0) && (liczba <=49)) {
		cout << "Ocena niedostateczna" << endl;
	
	} else if ((liczba >=50) && (liczna <=69)) {
	} cout << ""
    return 0;
}