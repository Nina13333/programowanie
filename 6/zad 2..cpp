#include <iostream>
using namespace std;
int main () {
	int tablica[5];
	int suma = 0;
	cout << "Podaj liczbe nr\n ";
	for (int i = 0; i < 5; i++) {
		cin >> tablica[i];
	
	if(tablica <= 0) cout << 0;
	       
	else (cout << i);
	}
	cout << "Liczby ujemne zamienione na 0: " << suma << endl;
	
		
}
